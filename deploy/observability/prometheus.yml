global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: zbot
    # ZBOT exposes /metrics on port 9091 (to be added in Phase 3)
    static_configs:
      - targets: ["host.docker.internal:9091"]
        labels:
          app: zbot
          env: local
