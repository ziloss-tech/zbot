# Copy to .env and fill in values
GRAFANA_PASSWORD=yourpasswordhere

# ZBOT log directory — must match the volume mount in docker-compose.yml
# Create this directory before starting the stack
ZBOT_LOG_DIR=/tmp/zbot-logs
